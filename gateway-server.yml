spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true


management:
  endpoint:
    gateway:
      access: unrestricted

info:
  app:
    name: "gateway-server"
    description: "edge server to my ecommerce microservice network"


server:
  port: 9090
